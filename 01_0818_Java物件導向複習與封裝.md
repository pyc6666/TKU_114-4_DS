# 8/18 教材：Java 物件導向複習與封裝

## 單元名稱d

物件狀態、建構子、封裝、物件參考與 composition

## 課程定位

本單元不是重新介紹所有 Java 基礎語法，而是整理後續資料結構會反覆使用的物件導向能力。Tree、Heap、Graph 都會由多個物件共同組成；若 class 的責任、狀態與操作沒有先設計清楚，資料結構愈大愈難除錯。

## 學習目標

完成本單元後，應能：

1. 以 state 與 behavior 說明物件的責任。
2. 使用 constructor 建立合法的初始狀態。
3. 使用 `private`、getter 與 domain method 保護資料。
4. 區分 instance member 與 `static` member。
5. 說明 object reference、alias 與 `null`。
6. 使用 composition 組合多個類別。
7. 將平行陣列重構為物件集合。

## 先備知識

- Java 變數、條件判斷、迴圈與 method。
- class、object 與 constructor 的基本語法。
- 陣列與 enhanced `for` loop。
- 建立 `0818` 資料夾，所有範例依標示檔名存放。

## 問題情境

如果商品名稱、價格與庫存分散在三個陣列，程式必須靠相同索引維持資料關係。刪除或排序其中一個陣列時，只要其他陣列沒有同步更新，資料就會錯位。

物件導向設計會把同一筆資料與管理資料的方法放在同一個 class 中。例如 `Product` 保存 `id`、`name`、`price` 與 `stock`，並由 `restock()`、`sell()` 控制庫存變化。後續的 List、Tree 與 Hash Table 儲存的是完整 `Product` 物件，而不是互相依賴的零散陣列。

## 核心概念

### 概念 1：用 state 與 behavior 描述物件

#### 概念說明

物件是相關 state 與 behavior 的組合。State 是物件目前保存的資料，通常由 field 表示；behavior 是物件可以執行的操作，由 instance method 表示。

設計 class 時，不要先問「要寫幾個 getter」，而要先問：

1. 這個物件代表什麼？
2. 哪些資料必須一起存在？
3. 哪些規則應由物件自己維護？

例如銀行帳戶的餘額不應由外部程式任意指定，應透過 `deposit()` 與 `withdraw()` 改變。這樣餘額不得為負數的規則只需要集中管理一次。

#### 實際應用

- 訂單物件保存編號、客戶與金額，並判斷是否可以取消。
- Tree node 保存資料與子節點 reference。
- Graph vertex 保存頂點資料與相鄰頂點。

#### 資料變化

`deposit(500)` 不是單純把 `balance` 顯示成 500，而是讓物件從原狀態轉移到新狀態：

```text
原餘額 1000 -> 驗證存款金額 500 -> 新餘額 1500
```

#### 何時適合使用

當多個資料欄位屬於同一筆資料，且資料變化有規則時，應建立 class。只有單一、短暫且沒有行為的區域值，不需要為了形式強行建立物件。

#### 範例程式

檔名：`BankAccountReview.java`

```java
class BankAccount {
    private String accountId;
    private int balance;

    BankAccount(String accountId, int openingBalance) {
        this.accountId = accountId;
        this.balance = Math.max(0, openingBalance);
    }

    void deposit(int amount) {
        if (amount > 0) {
            balance += amount;
        }
    }

    boolean withdraw(int amount) {
        if (amount <= 0 || amount > balance) {
            return false;
        }
        balance -= amount;
        return true;
    }

    String getAccountId() {
        return accountId;
    }

    int getBalance() {
        return balance;
    }
}

public class BankAccountReview {
    public static void main(String[] args) {
        BankAccount account = new BankAccount("A101", 1000);

        account.deposit(500);
        System.out.println("提款 300：" + account.withdraw(300));
        System.out.println("提款 5000：" + account.withdraw(5000));
        System.out.println(account.getAccountId() + " 餘額：" +
            account.getBalance());
    }
}
```

執行結果：

```text
提款 300：true
提款 5000：false
A101 餘額：1200
```

#### 執行重點

觀察 `balance` 只能透過物件提供的方法改變。失敗的提款不應改變餘額。

---

### 概念 2：Constructor 建立合法初始狀態

#### 概念說明

Constructor 在 `new` 物件時執行，名稱與 class 相同，沒有回傳型態。它的工作不只是把參數複製到 field，而是確保物件建立完成時已符合基本規則。

`this.field = parameter` 中，`this` 表示目前正在建立的物件。當 field 與 parameter 同名時，`this` 可以清楚區分兩者。

如果無效資料可以進入物件，後續每個 method 都必須再次防守。將不變條件集中在 constructor 與修改狀態的方法，可以降低錯誤擴散。

#### 實際應用

- 商品價格與庫存不得小於 0。
- Tree node 建立時必須保存一筆資料。
- 工作項目的 id 不得為空字串。

#### 資料變化

```text
輸入名稱 "  Keyboard  " -> trim() -> "Keyboard"
輸入價格 -500 -> 驗證 -> 0
```

#### 何時適合使用

只要物件建立時有必要資料，就應透過 constructor 傳入。可有可無的資料可以保留預設值，或由明確的 method 在建立後設定。

#### 範例程式

檔名：`ConstructorValidationDemo.java`

```java
class CourseItem {
    private String name;
    private int points;

    CourseItem(String name, int points) {
        this.name = name == null ? "Unnamed" : name.trim();
        if (this.name.isEmpty()) {
            this.name = "Unnamed";
        }
        this.points = Math.max(0, points);
    }

    String describe() {
        return name + "：" + points + " 分";
    }
}

public class ConstructorValidationDemo {
    public static void main(String[] args) {
        CourseItem first = new CourseItem("  Tree Practice  ", 20);
        CourseItem second = new CourseItem("   ", -5);

        System.out.println(first.describe());
        System.out.println(second.describe());
    }
}
```

執行結果：

```text
Tree Practice：20 分
Unnamed：0 分
```

#### 執行重點

比較傳入值與物件實際保存的值。Constructor 完成後，兩個物件都處於可使用狀態。

---

### 概念 3：Encapsulation 不等於只有 getter 與 setter

#### 概念說明

Encapsulation 的重點是把資料與規則包在 class 內，控制外部程式可以做什麼。將 field 設成 `private` 是第一步，但如果每個 field 都提供完全不驗證的 setter，外部仍然可以破壞物件狀態。

應優先提供有業務意義的 domain method，例如 `sell(quantity)`、`restock(quantity)`、`complete()`。這些 method 直接表達操作目的，也能集中處理驗證。

#### 實際應用

- 商品只能在庫存足夠時銷售。
- 任務只能從等待狀態轉為完成狀態。
- 節點連結只能由 Linked List 的操作方法調整。

#### 資料變化

```text
stock = 8
sell(3) -> true  -> stock = 5
sell(9) -> false -> stock 仍為 5
```

#### 何時適合使用

只要 field 的變更有條件或會連帶影響其他資料，就不應提供單純 setter。只有沒有額外規則、確實允許任意替換的資料才適合一般 setter。

#### 範例程式

檔名：`ProductEncapsulationDemo.java`

```java
class Product {
    private String id;
    private String name;
    private int stock;

    Product(String id, String name, int stock) {
        this.id = id;
        this.name = name;
        this.stock = Math.max(0, stock);
    }

    boolean sell(int quantity) {
        if (quantity <= 0 || quantity > stock) {
            return false;
        }
        stock -= quantity;
        return true;
    }

    void restock(int quantity) {
        if (quantity > 0) {
            stock += quantity;
        }
    }

    int getStock() {
        return stock;
    }

    @Override
    public String toString() {
        return id + " " + name + " stock=" + stock;
    }
}

public class ProductEncapsulationDemo {
    public static void main(String[] args) {
        Product product = new Product("P101", "Keyboard", 8);

        System.out.println("銷售 3 個：" + product.sell(3));
        System.out.println("銷售 9 個：" + product.sell(9));
        product.restock(4);

        System.out.println(product);
    }
}
```

執行結果：

```text
銷售 3 個：true
銷售 9 個：false
P101 Keyboard stock=9
```

#### 執行重點

外部程式只能提出銷售或補貨要求，是否接受由 `Product` 自己判斷。

---

### 概念 4：Instance member 與 static member

#### 概念說明

Instance field 屬於各自的物件，每個物件都有獨立值；`static` field 屬於 class，所有物件共用同一份資料。

如果把每個帳戶自己的餘額設成 `static`，所有帳戶會共用同一個餘額，這通常是嚴重錯誤。適合 `static` 的資料包括物件總數、固定常數，以及不依賴任何物件狀態的工具方法。

#### 實際應用

- 自動產生流水編號。
- 記錄目前建立過多少個 node。
- 宣告固定容量或固定狀態名稱。

#### 資料變化

```text
new Ticket("Network") -> nextNumber 由 1 變 2
new Ticket("Printer") -> nextNumber 由 2 變 3
```

每張 ticket 的 `number` 不同，但它們共用 `nextNumber`。

#### 何時適合使用

資料確實屬於整個 class，而不是某個個別物件時才使用 `static`。判斷方式是問：「建立兩個物件時，這個值應該各自擁有，還是共同擁有？」

#### 範例程式

檔名：`StaticMemberReview.java`

```java
class SupportTicket {
    private static int nextNumber = 1;

    private int number;
    private String issue;

    SupportTicket(String issue) {
        number = nextNumber;
        nextNumber++;
        this.issue = issue;
    }

    static int getCreatedCount() {
        return nextNumber - 1;
    }

    @Override
    public String toString() {
        return "T" + number + " " + issue;
    }
}

public class StaticMemberReview {
    public static void main(String[] args) {
        SupportTicket first = new SupportTicket("Network");
        SupportTicket second = new SupportTicket("Printer");

        System.out.println(first);
        System.out.println(second);
        System.out.println("建立數量：" + SupportTicket.getCreatedCount());
    }
}
```

執行結果：

```text
T1 Network
T2 Printer
建立數量：2
```

#### 執行重點

`getCreatedCount()` 使用 class 名稱呼叫，因為它不屬於任何單一 ticket。

---

### 概念 5：Object reference、alias 與 null

#### 概念說明

Java 的物件變數保存的是 reference，不是把整個物件內容直接放進變數。兩個變數可以指向同一個物件，這種情況稱為 alias。透過其中一個 reference 修改物件，另一個 reference 看到的狀態也會改變。

`null` 表示目前沒有指向任何物件。對 `null` 呼叫 method 或讀取 field 會產生 `NullPointerException`。

#### 實際應用

- Linked List 的 `head` 指向第一個 node，空串列時為 `null`。
- Tree node 的 `left`、`right` 指向子節點，沒有子節點時為 `null`。
- Queue 中保存的 object reference 與原始清單可能指向同一個任務物件。

#### 資料變化

```text
first ----+
          +----> ScoreRecord(score=70)
second ---+

second.addBonus(10)

first.getScore() 也會得到 80
```

#### 何時適合使用

需要多個結構共同操作同一物件時，reference sharing 很有用；若需要互不影響的資料，應建立新物件或 copy，而不是只複製 reference。

#### 範例程式

檔名：`ObjectReferenceReview.java`

```java
class ScoreRecord {
    private String name;
    private int score;

    ScoreRecord(String name, int score) {
        this.name = name;
        this.score = score;
    }

    void addBonus(int points) {
        if (points > 0) {
            score += points;
        }
    }

    int getScore() {
        return score;
    }

    ScoreRecord copy() {
        return new ScoreRecord(name, score);
    }
}

public class ObjectReferenceReview {
    public static void main(String[] args) {
        ScoreRecord first = new ScoreRecord("Amy", 70);
        ScoreRecord alias = first;
        ScoreRecord copy = first.copy();

        alias.addBonus(10);

        System.out.println("first：" + first.getScore());
        System.out.println("alias：" + alias.getScore());
        System.out.println("copy：" + copy.getScore());
        System.out.println("first == alias：" + (first == alias));
        System.out.println("first == copy：" + (first == copy));
    }
}
```

執行結果：

```text
first：80
alias：80
copy：70
first == alias：true
first == copy：false
```

#### 執行重點

`==` 比較兩個 reference 是否指向同一物件，不是比較物件內容是否相同。

---

### 概念 6：Composition 表達 has-a 關係

#### 概念說明

Composition 是讓一個物件包含或使用另一個物件。若 `Order` 擁有一個 `Customer`，可以描述為 Order has a Customer。這與 inheritance 的 is-a 關係不同，下一單元會進一步比較。

Composition 可以把大型問題拆成責任清楚的小類別。`Customer` 管理客戶資料，`Order` 管理訂單狀態與金額，不必把所有 field 與 method 塞進同一個 class。

#### 實際應用

- `BinaryTree` has a root node。
- `Graph` has a collection of vertices。
- `Order` has a customer and order items。

#### 資料變化

`Order` 保存的是 `Customer` reference。更新 customer 的會員等級後，order 透過同一 reference 取得的資料也會更新。

#### 何時適合使用

當兩個概念有「擁有、包含、使用」關係，但不是同一種類型時，優先考慮 composition。Composition 通常比不必要的 inheritance 更容易替換與測試。

#### 範例程式

檔名：`OrderCompositionDemo.java`

```java
class Customer {
    private String id;
    private String name;

    Customer(String id, String name) {
        this.id = id;
        this.name = name;
    }

    String label() {
        return id + " " + name;
    }
}

class Order {
    private String orderId;
    private Customer customer;
    private int total;

    Order(String orderId, Customer customer, int total) {
        this.orderId = orderId;
        this.customer = customer;
        this.total = Math.max(0, total);
    }

    String summary() {
        return orderId + " | " + customer.label() + " | $" + total;
    }
}

public class OrderCompositionDemo {
    public static void main(String[] args) {
        Customer customer = new Customer("C101", "Amy");
        Order order = new Order("O9001", customer, 2500);

        System.out.println(order.summary());
    }
}
```

執行結果：

```text
O9001 | C101 Amy | $2500
```

#### 執行重點

`Order` 不重複保存 customer id 與 name，而是保存一個完整的 `Customer` reference。

---

### 概念 7：用物件陣列取代平行陣列

#### 概念說明

平行陣列使用相同索引表示同一筆資料，例如 `ids[0]`、`names[0]`、`scores[0]`。這種寫法缺乏結構，只要其中一個陣列排序或刪除不同步，資料就會錯位。

物件陣列把每一筆資料包成一個完整物件。陣列只需要保存 object reference，走訪、搜尋與排序時也只需移動一個 reference。

#### 實際應用

- 後續使用 `ArrayList<StudentRecord>` 管理動態名單。
- BST node 保存完整商品或任務物件。
- HashMap 以 id 對應完整資料物件。

#### 資料變化

```text
平行陣列：ids[1], names[1], scores[1]
物件陣列：records[1] -> StudentRecord(id, name, score)
```

#### 何時適合使用

當一筆資料由兩個以上相關欄位組成時，通常應建立 class。大量數值計算且資料欄位非常單純時，primitive array 仍可能比較直接。

#### 範例程式

檔名：`ObjectArrayReview.java`

```java
class StudentRecord {
    private String id;
    private String name;
    private int score;

    StudentRecord(String id, String name, int score) {
        this.id = id;
        this.name = name;
        this.score = score;
    }

    boolean passed() {
        return score >= 60;
    }

    int getScore() {
        return score;
    }

    @Override
    public String toString() {
        return id + " " + name + " score=" + score;
    }
}

public class ObjectArrayReview {
    public static void main(String[] args) {
        StudentRecord[] records = {
            new StudentRecord("S101", "Amy", 82),
            new StudentRecord("S102", "Ben", 55),
            new StudentRecord("S103", "Cara", 91)
        };

        int total = 0;
        for (StudentRecord record : records) {
            total += record.getScore();
            System.out.println(record + " passed=" + record.passed());
        }

        System.out.println("平均：" + total / records.length);
    }
}
```

執行結果：

```text
S101 Amy score=82 passed=true
S102 Ben score=55 passed=false
S103 Cara score=91 passed=true
平均：76
```

#### 執行重點

陣列中的每一格保存一個 `StudentRecord` reference。走訪時取得的是完整物件，不必同步三個陣列。

---

### 概念 8：`this` 與 constructor overloading

#### 概念說明

`this` 代表「目前正在執行 method 的這個物件」。當 parameter 與 field 同名時，`this.name = name` 左邊的 `this.name` 是 field，右邊的 `name` 是 parameter。

同一個 class 可以有多個 constructor，只要 parameter list 不同。這種做法稱為 constructor overloading。為了避免每個 constructor 都重複驗證邏輯，簡化版 constructor 可以使用 `this(...)` 呼叫完整版 constructor。

`this(...)` 必須是 constructor 的第一個 statement。否則物件可能在完整初始化前就執行其他操作。

#### 實際應用

- 建立課程時允許使用預設容量。
- 建立帳號時允許初始餘額為 0。
- 建立商品時允許尚未指定分類。

#### 資料變化

```text
new CourseSection("CS101", "Java")
        -> this("CS101", "Java", 30)
        -> code="CS101", title="Java", capacity=30
```

#### 設計判斷

Constructor overloading 適合「同一種物件，但有數種合理建立方式」。如果參數太多、同為 `String` 而難以分辨，過多 overloaded constructor 反而容易傳錯順序。

#### 範例程式

檔名：`ConstructorOverloadingDemo.java`

```java
class CourseSection {
    private String code;
    private String title;
    private int capacity;
    private int enrolled;

    CourseSection(String code, String title) {
        this(code, title, 30);
    }

    CourseSection(String code, String title, int capacity) {
        this.code = code == null || code.isBlank() ? "UNKNOWN" : code;
        this.title = title == null || title.isBlank() ? "Untitled" : title;
        this.capacity = capacity > 0 ? capacity : 30;
        this.enrolled = 0;
    }

    boolean enroll() {
        if (enrolled >= capacity) {
            return false;
        }
        enrolled++;
        return true;
    }

    @Override
    public String toString() {
        return code + " " + title + " " + enrolled + "/" + capacity;
    }
}

public class ConstructorOverloadingDemo {
    public static void main(String[] args) {
        CourseSection regular = new CourseSection("CS101", "Java");
        CourseSection workshop = new CourseSection("DS201", "Tree Lab", 2);

        regular.enroll();
        workshop.enroll();
        workshop.enroll();

        System.out.println(regular);
        System.out.println(workshop);
        System.out.println("third enrollment=" + workshop.enroll());
    }
}
```

執行結果：

```text
CS101 Java 1/30
DS201 Tree Lab 2/2
third enrollment=false
```

#### 實作變化

新增一個只接受 `code` 的 constructor，title 預設為 `Independent Study`，capacity 預設為 10，並且必須透過 `this(...)` 完成初始化。

---

### 概念 9：Object reference 作為 method parameter

#### 概念說明

Java 的 parameter 一律使用 pass-by-value。當傳入 primitive value，method 得到數值的 copy；當傳入 object reference，method 得到的是 reference value 的 copy。兩個 reference 仍指向同一個物件，因此 method 可以透過 reference 修改該物件。

但是，method 內如果將 parameter 重新指向新物件，不會改變 caller 原本的 reference。這是「能修改同一個 object」與「不能替 caller 換掉 reference」的關鍵差異。

#### 實際應用

- Service method 接收帳戶並完成轉帳。
- Method 接收訂單，檢查後更新狀態。
- Sorting method 交換陣列中的 object reference。

#### 資料變化

| 步驟        | `source` 指向 | `target` 指向 | 餘額                        |
| ----------- | ------------- | ------------- | --------------------------- |
| 呼叫前      | A 帳戶        | B 帳戶        | A=1000, B=200               |
| 進入 method | A 帳戶        | B 帳戶        | 兩個 reference value 被複製 |
| 完成轉帳    | A 帳戶        | B 帳戶        | A=700, B=500                |

#### 設計判斷

Method 可以接收物件來協調多個物件的操作，但不應該隨意直接改 field。轉帳應呼叫帳戶提供的 `withdraw` 與 `deposit`，讓驗證邏輯留在帳戶類別中。

#### 範例程式

檔名：`ObjectParameterDemo.java`

```java
class TransferAccount {
    private String id;
    private int balance;

    TransferAccount(String id, int balance) {
        this.id = id;
        this.balance = Math.max(0, balance);
    }

    boolean withdraw(int amount) {
        if (amount <= 0 || amount > balance) {
            return false;
        }
        balance -= amount;
        return true;
    }

    void deposit(int amount) {
        if (amount > 0) {
            balance += amount;
        }
    }

    @Override
    public String toString() {
        return id + " balance=" + balance;
    }
}

public class ObjectParameterDemo {
    static boolean transfer(TransferAccount source,
                            TransferAccount target,
                            int amount) {
        if (source == null || target == null || source == target) {
            return false;
        }
        if (!source.withdraw(amount)) {
            return false;
        }
        target.deposit(amount);
        return true;
    }

    static void replaceLocally(TransferAccount account) {
        account = new TransferAccount("LOCAL", 9999);
        System.out.println("method 內：" + account);
    }

    public static void main(String[] args) {
        TransferAccount a = new TransferAccount("A", 1000);
        TransferAccount b = new TransferAccount("B", 200);

        System.out.println("transfer=" + transfer(a, b, 300));
        System.out.println(a);
        System.out.println(b);

        replaceLocally(a);
        System.out.println("method 外：" + a);
    }
}
```

執行結果：

```text
transfer=true
A balance=700
B balance=500
method 內：LOCAL balance=9999
method 外：A balance=700
```

#### 執行重點

`replaceLocally` 只改變自己的 parameter，因此 `main` 中的 `a` 仍然指向原本帳戶。

---

### 概念 10：Immutable object 與 defensive copy

#### 概念說明

Immutable object 建立後不再改變內部狀態。基本做法包含：field 設為 `private final`、不提供會修改 field 的 method，並且不把內部 mutable object 直接暴露給外部。

即使 field 本身是 `final`，如果它指向陣列，陣列元素仍可以被改動。因此 constructor 接收陣列時要複製，getter 回傳陣列時也要複製，這稱為 defensive copy。

#### 實際應用

- 完成交易後的付款紀錄。
- 發佈後不可直接篡改的成績快照。
- 多個物件共用但不應被改動的設定值。

#### 資料變化

```text
sourceScores = [80, 90, 70]
constructor 複製 -> snapshot 內部 [80, 90, 70]
sourceScores[0] = 0
snapshot 內部仍為 [80, 90, 70]
```

#### 設計判斷

不是每個 class 都應該 immutable。帳戶餘額、庫存數量這類核心就是狀態變化的物件，適合使用有驗證的 domain method。交易紀錄、快照與 id 值物件通常更適合 immutable。

#### 範例程式

檔名：`DefensiveCopyDemo.java`

```java
import java.util.Arrays;

final class ScoreSnapshot {
    private final String course;
    private final int[] scores;

    ScoreSnapshot(String course, int[] scores) {
        this.course = course == null ? "Unknown" : course;
        this.scores = scores == null
                ? new int[0]
                : Arrays.copyOf(scores, scores.length);
    }

    int[] getScores() {
        return Arrays.copyOf(scores, scores.length);
    }

    double average() {
        if (scores.length == 0) {
            return 0.0;
        }
        int total = 0;
        for (int score : scores) {
            total += score;
        }
        return (double) total / scores.length;
    }

    @Override
    public String toString() {
        return course + " " + Arrays.toString(scores);
    }
}

public class DefensiveCopyDemo {
    public static void main(String[] args) {
        int[] original = {80, 90, 70};
        ScoreSnapshot snapshot = new ScoreSnapshot("Java", original);

        original[0] = 0;
        int[] received = snapshot.getScores();
        received[1] = 0;

        System.out.println("original=" + Arrays.toString(original));
        System.out.println("received=" + Arrays.toString(received));
        System.out.println("snapshot=" + snapshot);
        System.out.printf("average=%.1f%n", snapshot.average());
    }
}
```

執行結果：

```text
original=[0, 90, 70]
received=[80, 0, 70]
snapshot=Java [80, 90, 70]
average=80.0
```

#### 實作變化

新增 `highestScore()`；空陣列回傳 `-1`，其他情況回傳最高成績。

---

### 概念 11：`toString`、`equals` 與物件身分

#### 概念說明

`toString()` 用來表示物件的可讀摘要。當物件與字串使用 `+` 連接，或直接傳給 `System.out.println`，Java 會呼叫 `toString()`。

`==` 用於 object reference 時比較「是否指向同一個物件」；`equals()` 通常用來表示「兩個物件在 domain 上是否視為相等」。例如兩個不同的 `Member` object 只要 member id 相同，可以視為同一位會員。

Override `equals()` 時原則上也要 override `hashCode()`。8/20 使用 `HashSet` 與 `HashMap` 時會實際觀察兩者的關係。

#### 實際應用

- 判斷同一學號、訂單編號或商品編號。
- 輸出 log 或除錯資訊。
- 將物件放入 Set 或作為 Map key。

#### 資料變化

```text
a == b       -> 比較 reference
a.equals(b)  -> 比較 Member 定義的 id
```

#### 設計判斷

`equals()` 應使用能穩定表示 domain identity 的欄位。如果以可變動的 name 或 email 判斷身分，修改資料後同一個實體可能被誤認為不同物件。只需求比較是否同一 reference 時使用 `==`，需求比較 domain identity 時才使用 `equals()`。

#### 範例程式

檔名：`ObjectEqualityDemo.java`

```java
import java.util.Objects;

class Member {
    private final String id;
    private String name;

    Member(String id, String name) {
        this.id = id;
        this.name = name;
    }

    @Override
    public String toString() {
        return "Member{id='" + id + "', name='" + name + "'}";
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) {
            return true;
        }
        if (!(other instanceof Member member)) {
            return false;
        }
        return Objects.equals(id, member.id);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id);
    }
}

public class ObjectEqualityDemo {
    public static void main(String[] args) {
        Member a = new Member("M001", "Amy");
        Member b = new Member("M001", "Amy Chen");
        Member c = a;

        System.out.println(a);
        System.out.println("a == b: " + (a == b));
        System.out.println("a.equals(b): " + a.equals(b));
        System.out.println("a == c: " + (a == c));
    }
}
```

執行結果：

```text
Member{id='M001', name='Amy'}
a == b: false
a.equals(b): true
a == c: true
```

#### 執行重點

`a` 與 `b` 是不同 object，但 id 相同，所以 `equals()` 回傳 `true`。`c` 直接複製 `a` 的 reference，因此 `a == c` 為 `true`。

---

### 概念 12：綜合應用，電子錢包與交易紀錄

#### 問題情境

電子錢包需要保護餘額，每次儲值或付款都要留下不可修改的交易紀錄。這個案例同時使用 encapsulation、constructor validation、composition、object array、immutable record 與 `toString()`。

#### 實際應用

- 電子支付與儲值金系統。
- 需要保留餘額變化軌跡的帳戶。
- 以不可變紀錄支援後續稽核與報表。

#### 設計分工

- `WalletTransaction`：一筆已發生的交易，建立後不可改動。
- `DigitalWallet`：管理餘額、驗證操作與保存交易。
- `WalletTransactionSystem`：建立物件並執行完整測試情境。

#### 設計判斷

餘額是可變狀態，必須由 `DigitalWallet` 的 domain method 控制；已發生交易是歷史事實，適合使用 immutable object。固定長度陣列可用於尚未介紹 List 時練習容量與計數，實際系統則要評估動態 collection 或持久化儲存。

#### 資料變化

| 操作      | 操作前餘額 | 結果 | 操作後餘額 | 紀錄數 |
| --------- | ---------: | ---- | ---------: | -----: |
| 儲值 1000 |          0 | 成功 |       1000 |      1 |
| 付款 250  |       1000 | 成功 |        750 |      2 |
| 付款 900  |        750 | 失敗 |        750 |      2 |
| 退款 50   |        750 | 成功 |        800 |      3 |

#### 範例程式

檔名：`WalletTransactionSystem.java`

```java
final class WalletTransaction {
    private final int sequence;
    private final String type;
    private final int amount;
    private final int balanceAfter;

    WalletTransaction(int sequence, String type, int amount, int balanceAfter) {
        this.sequence = sequence;
        this.type = type;
        this.amount = amount;
        this.balanceAfter = balanceAfter;
    }

    @Override
    public String toString() {
        return sequence + " " + type + " " + amount
                + " balance=" + balanceAfter;
    }
}

class DigitalWallet {
    private final String walletId;
    private final String owner;
    private int balance;
    private final WalletTransaction[] transactions;
    private int transactionCount;

    DigitalWallet(String walletId, String owner, int historyCapacity) {
        this.walletId = walletId == null || walletId.isBlank()
                ? "UNKNOWN" : walletId;
        this.owner = owner == null || owner.isBlank() ? "Unknown" : owner;
        this.balance = 0;
        this.transactions = new WalletTransaction[Math.max(1, historyCapacity)];
        this.transactionCount = 0;
    }

    boolean deposit(int amount) {
        if (amount <= 0 || transactionCount >= transactions.length) {
            return false;
        }
        balance += amount;
        record("DEPOSIT", amount);
        return true;
    }

    boolean pay(int amount) {
        if (amount <= 0 || amount > balance
                || transactionCount >= transactions.length) {
            return false;
        }
        balance -= amount;
        record("PAY", amount);
        return true;
    }

    boolean refund(int amount) {
        if (amount <= 0 || transactionCount >= transactions.length) {
            return false;
        }
        balance += amount;
        record("REFUND", amount);
        return true;
    }

    private void record(String type, int amount) {
        transactions[transactionCount] = new WalletTransaction(
                transactionCount + 1, type, amount, balance);
        transactionCount++;
    }

    void printStatement() {
        System.out.println(walletId + " owner=" + owner
                + " balance=" + balance);
        for (int i = 0; i < transactionCount; i++) {
            System.out.println(transactions[i]);
        }
    }
}

public class WalletTransactionSystem {
    public static void main(String[] args) {
        DigitalWallet wallet = new DigitalWallet("W001", "Amy", 5);

        System.out.println("deposit=" + wallet.deposit(1000));
        System.out.println("pay 250=" + wallet.pay(250));
        System.out.println("pay 900=" + wallet.pay(900));
        System.out.println("refund=" + wallet.refund(50));
        wallet.printStatement();
    }
}
```

執行結果：

```text
deposit=true
pay 250=true
pay 900=false
refund=true
W001 owner=Amy balance=800
1 DEPOSIT 1000 balance=1000
2 PAY 250 balance=750
3 REFUND 50 balance=800
```

#### 實作變化

新增 `transferTo(DigitalWallet target, int amount)`。轉帳成功時，來源錢包記錄 `TRANSFER_OUT`，目標錢包記錄 `TRANSFER_IN`；任一錢包無空間、金額不合法或餘額不足時，兩邊都不得改變。

## 程式執行追蹤

### 追蹤一：Alias 與狀態變化

```java
TransferAccount first = new TransferAccount("A", 500);
TransferAccount second = first;
second.deposit(200);
```

| 步驟                  | `first` | `second` | A 帳戶餘額 |
| --------------------- | ------- | -------- | ---------: |
| 建立帳戶              | 指向 A  | 尚未存在 |        500 |
| `second = first`      | 指向 A  | 指向 A   |        500 |
| `second.deposit(200)` | 指向 A  | 指向 A   |        700 |

`first` 與 `second` 並沒有各自保存餘額，它們共同指向同一個帳戶。

### 追蹤二：Defensive copy

```java
int[] source = {70, 80};
ScoreSnapshot snapshot = new ScoreSnapshot("DS", source);
source[0] = 0;
int[] copy = snapshot.getScores();
copy[1] = 0;
```

| 步驟             | `source`   | snapshot 內部 | `copy`    |
| ---------------- | ---------- | ------------- | --------- |
| 建立前           | `[70, 80]` | 不存在        | 不存在    |
| 建立後           | `[70, 80]` | `[70, 80]`    | 不存在    |
| 修改 source      | `[0, 80]`  | `[70, 80]`    | 不存在    |
| 修改 getter 結果 | `[0, 80]`  | `[70, 80]`    | `[70, 0]` |

## 除錯練習

### 除錯練習一：Constructor 沒有設定 field

```java
class BrokenProduct {
    private String name;

    BrokenProduct(String name) {
        name = name;
    }
}
```

問題：`name = name` 的左右兩邊都是 parameter，field 仍然是 `null`。

修正：

```java
this.name = name;
```

### 除錯練習二：Getter 暴露內部陣列

```java
int[] getScores() {
    return scores;
}
```

問題：Caller 取得內部陣列的 reference，可以繞過 class method 直接改動內容。

修正：

```java
return Arrays.copyOf(scores, scores.length);
```

## 課堂實作題

### 課堂實作題一：設備庫存物件

指定檔名：`EquipmentInventory.java`

建立 `Equipment` class，包含：

- `id`、`name`、`availableCount` 三個 private field。
- Constructor：空白 id 或 name 改為 `Unknown`，負數數量改為 0。
- `borrowOne()`：有庫存時減 1 並回傳 `true`，否則回傳 `false`。
- `returnItems(int quantity)`：正數才加入庫存。
- `toString()`：輸出設備編號、名稱與可借數量。

在 `EquipmentInventory` 的 `main()` 建立兩個設備，測試借用成功、借用失敗與歸還。

完成標準：程式可執行，失敗操作不會讓庫存變成負數。

### 課堂實作題二：課程與授課者 Composition

指定檔名：`CourseComposition.java`

建立 `Instructor` 與 `Course`：

- `Instructor` 保存 id 與 name。
- `Course` 保存 courseCode、title 與一個 `Instructor` reference。
- `Course.summary()` 回傳完整課程資訊。
- 建立至少兩門課，兩門課可以共用同一個 instructor。

完成標準：不得在 `Course` 重複保存 instructorName；必須透過 composition 取得。

### 課堂實作題三：物件陣列統計

指定檔名：`BookArrayReport.java`

建立 `Book` class，包含書號、書名、價格及庫存。以 `Book[]` 保存至少四本書，完成：

1. 輸出所有書籍。
2. 計算庫存總價值 `price * stock`。
3. 找出價格最高的書。
4. 輸出庫存小於或等於 3 的書。

完成標準：不得使用四個平行陣列；每筆資料必須是完整 `Book` 物件。

### 課堂實作題四：會員身分比較

指定檔名：`MemberEqualityPractice.java`

建立 `LibraryMember`，包含 `memberId`、`name` 與 `email`。完成：

1. Override `toString()` 輸出所有欄位。
2. Override `equals()` 與 `hashCode()`，只使用 `memberId` 判斷身分。
3. 建立兩個 id 相同但 email 不同的物件。
4. 輸出 `==` 與 `equals()` 的比較結果。

邊界條件：與 `null` 比較必須回傳 `false`，不可發生例外。

### 課堂實作題五：Immutable 庫存快照

指定檔名：`InventorySnapshotPractice.java`

建立 immutable `InventorySnapshot`，包含 `warehouseId` 與 `int[] quantities`。

- Constructor 與 getter 都必須 defensive copy。
- `totalQuantity()` 回傳總數量。
- `outOfStockCount()` 回傳數量為 0 的品項數。
- 使用 `{5, 0, 3, 0}` 測試，總數為 8，缺貨品項為 2。

邊界條件：Constructor 收到 `null` 陣列時，建立長度為 0 的內部陣列。

## 課後作業

### 課後作業一：封裝式電子錢包

指定檔名：`DigitalWalletSystem.java`

建立 `DigitalWallet`，包含 walletId、owner、balance。完成儲值、付款、退款及交易次數統計。付款金額不合法或餘額不足時不得改變狀態。

預期至少測試：正常儲值、正常付款、餘額不足、負數金額與退款。

### 課後作業二：訂單與顧客管理

指定檔名：`CustomerOrderSystem.java`

使用 composition 建立 `Customer`、`OrderItem` 與 `CustomerOrder`。訂單包含一位顧客及多個固定長度的 `OrderItem[]`，完成訂單總額、品項數量及摘要輸出。

完成標準：訂單不得把顧客與品項資料拆成平行陣列。

### 課後作業三：課程成績物件系統

指定檔名：`CourseGradeManager.java`

建立 `CourseGrade` class，保存學號、姓名、平時、期中與期末成績。各成績限制為 0 到 100，並提供：

- `calculateFinalScore()`：平時 50%、期中 20%、期末 20%、出席 10%。
- `getLevel()`：依總分回傳 A、B、C、D 或 F。
- `toString()`：輸出完整摘要。

使用物件陣列保存至少五筆資料，輸出平均、最高分與不及格名單。

### 課後作業四：跨帳戶轉帳服務

指定檔名：`AccountTransferService.java`

建立 `Account` 與 `TransferService`。`TransferService.transfer(Account source, Account target, int amount)` 必須確保：

- 來源與目標不是 `null`。
- 來源與目標不是同一個物件。
- 金額大於 0 且來源餘額足夠。
- 任一驗證失敗時，兩個帳戶都不得改變。

至少測試成功轉帳、餘額不足、同帳戶轉帳與 `null` 目標。

### 課後作業五：電子錢包交易系統擴充

指定檔名：`WalletHistoryManager.java`

以 `WalletTransactionSystem.java` 為基礎，完成：

1. `findTransaction(int sequence)`：找到時回傳交易，否則回傳 `null`。
2. `totalByType(String type)`：計算指定交易類型的總金額。
3. `transferTo(...)`：來源與目標同時留下紀錄。
4. 交易陣列已滿時不得修改餘額。
5. 輸出兩個錢包的完整 statement。

不可在 `main()` 直接修改 balance 或 transaction array。

## 常見錯誤與診斷

| 問題                   | 常見原因                                      | 修正方式                                      |
| ---------------------- | --------------------------------------------- | --------------------------------------------- |
| `NullPointerException` | reference 為`null` 卻呼叫 method              | 呼叫前確認物件是否已建立，追蹤 reference 指向 |
| Constructor 沒有執行   | Constructor 拼字與 class 不同，或誤寫回傳型態 | Constructor 必須與 class 同名且沒有回傳型態   |
| 所有物件資料一起改變   | 把 instance field 誤設成`static`              | 個別物件資料移除`static`                      |
| 外部可以建立負庫存     | 提供無驗證 setter 或 field 不是 private       | 改用 domain method 並集中驗證                 |
| 修改 copy 卻影響原物件 | 只複製 reference，沒有建立新物件              | 實作 copy constructor 或`copy()`              |
| 多個陣列資料錯位       | 排序或刪除時只更新部分陣列                    | 將相關欄位封裝成物件                          |

## 形成性評量

1. `BankAccount a = new BankAccount(...); BankAccount b = a;` 中，建立了幾個物件？
2. 為什麼 `balance` 不適合設成 `static`？
3. `private field + public setter` 是否一定完成良好封裝？說明原因。
4. `Order` 包含 `Customer` 應使用 inheritance 還是 composition？
5. 對 `null` reference 呼叫 method 會發生什麼問題？
6. 說明物件陣列比平行陣列容易維護的原因。

## 評分規準

| 評分項目       | 完整達成                                            | 部分達成                 | 尚未達成                          |
| -------------- | --------------------------------------------------- | ------------------------ | --------------------------------- |
| Class 與責任   | field 與 method 集中表達單一責任                    | 可執行但責任混雜         | 仍以零散變數或平行陣列為主        |
| Encapsulation  | private field、合法 constructor、domain method 完整 | 只有部分驗證             | field 可被任意破壞                |
| Reference 理解 | 能處理 alias、copy 與 null                          | 一般資料正確但邊界不足   | 因 reference 錯誤無法執行         |
| Composition    | 類別關係清楚且沒有重複資料                          | 可以組合但責任不清       | 使用不合理 inheritance 或複製欄位 |
| 程式完整性     | 檔名正確、可編譯、輸出與要求一致                    | 有少量錯誤但主要功能存在 | 缺少主要 class、method 或無法編譯 |
| GitHub 繳交    | `0818` 資料夾、檔案及連結完整                       | 資料夾或紀錄不完整       | 連結錯誤或未 push                 |

## 參考教材

- [Dev.java：Objects, Classes, Interfaces, Packages, and Inheritance](https://dev.java/learn/oop/)
- [Java SE 17 API Specification](https://docs.oracle.com/en/java/javase/17/docs/api/)
